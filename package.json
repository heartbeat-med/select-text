{
  "name": "select-text",
  "main": "./lib/select-text",
  "version": "0.0.0",
  "description": "Select text inside \"\" or ''",
  "keywords": [
    "select",
    "text",
    "plugin",
    "atom"
  ],
  "activationCommands": {
    "atom-workspace": "select-text:select"
  },
  "repository": "https://github.com/heartbeat-med/select-text",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
  }
}
